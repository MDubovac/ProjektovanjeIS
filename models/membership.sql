CREATE TABLE mships(
    mship_id INT(11) AUTO_INCREMENT NOT NULL,
    mship_name VARCHAR(255) NOT NULL,
    mship_email VARCHAR(255) NOT NULL,
    mship_plan VARCHAR(255) NOT NULL,
    PRIMARY KEY(mship_id)
);